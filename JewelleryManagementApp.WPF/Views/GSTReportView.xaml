<UserControl x:Class="JewelleryManagementApp.WPF.Views.GSTReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="200" d:DesignWidth="300">
    <StackPanel Margin="20">
        <Button Content="Refresh" Command="{Binding RefreshCommand}" />
        <TextBlock Text="GST Report" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
        <TextBlock Text="{Binding TotalSales, StringFormat='Total Sales: {0:C}'}" />
        <TextBlock Text="{Binding TotalGST, StringFormat='Total GST: {0:C}'}" />
    </StackPanel>
</UserControl>
