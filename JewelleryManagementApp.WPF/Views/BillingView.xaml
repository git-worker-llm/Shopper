<UserControl x:Class="JewelleryManagementApp.WPF.Views.BillingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Form Section -->
        <StackPanel Grid.Column="0" Margin="0,0,20,0">
            <GroupBox Header="Billing Information" Padding="10">
                <StackPanel>
                    <TextBlock Text="Select Customer:"/>
                    <ComboBox ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" />

                    <TextBlock Text="Select Item:"/>
                    <ComboBox ItemsSource="{Binding AvailableItems}" SelectedItem="{Binding SelectedItem, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" />
                    <TextBlock Text="Quantity:"/>
                    <TextBox Text="{Binding Quantity}" />

                    <Button Content="Add to Bill" Command="{Binding AddCommand}" />
                    <Button Content="Save Bill" Command="{Binding SaveBillCommand}" Background="Green" />
                </StackPanel>
            </GroupBox>
        </StackPanel>

        <!-- Bill Display Section -->
        <StackPanel Grid.Column="1">
            <GroupBox Header="Current Bill" Padding="10">
                <StackPanel>
                    <DataGrid ItemsSource="{Binding CurrentBillItems}" AutoGenerateColumns="False" Height="300" Margin="0,0,0,10">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Item" Binding="{Binding Item.Name}" Width="*" />
                            <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="Auto" />
                            <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="Auto" />
                        </DataGrid.Columns>
                    </DataGrid>
                    <TextBlock Text="{Binding TotalAmount, StringFormat='Total: {0:C}'}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Right"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </Grid>
</UserControl>
