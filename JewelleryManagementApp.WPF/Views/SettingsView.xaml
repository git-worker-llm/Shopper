<UserControl x:Class="JewelleryManagementApp.WPF.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="20">
        <TextBlock Text="Settings" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
        <TextBlock Text="Shop Name:"/>
        <TextBox Text="{Binding Settings.ShopName}" />
        <TextBlock Text="GST Number:"/>
        <TextBox Text="{Binding Settings.GSTNumber}" />
        <TextBlock Text="Address:"/>
        <TextBox Text="{Binding Settings.Address}" />
        <TextBlock Text="Logo Path:"/>
        <TextBox Text="{Binding Settings.LogoPath}" />
        <Button Content="Save Settings" Command="{Binding SaveSettingsCommand}" />
    </StackPanel>
</UserControl>
