<UserControl x:Class="JewelleryManagementApp.WPF.Views.PrintTemplateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <TextBlock Text="Edit Invoice Template" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            <StackPanel Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="Shop Name:" Width="100"/>
                <TextBox Text="{Binding Template.ShopName, UpdateSourceTrigger=PropertyChanged}" Width="180"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="GST Number:" Width="100"/>
                <TextBox Text="{Binding Template.GSTNumber, UpdateSourceTrigger=PropertyChanged}" Width="180"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="Address:" Width="100"/>
                <TextBox Text="{Binding Template.Address, UpdateSourceTrigger=PropertyChanged}" Width="180" Height="60" TextWrapping="Wrap"/>
            </StackPanel>
            <TextBlock Text="Template XAML:" Margin="0,10,0,0"/>
            <TextBox x:Name="TemplateXamlBox" Text="{Binding Template.TemplateXaml, UpdateSourceTrigger=PropertyChanged}" TextChanged="TextBox_TextChanged" Width="280" Height="200" TextWrapping="Wrap" AcceptsReturn="True" FontFamily="Consolas"/>

            <TextBlock Text="Insert Field:" Margin="0,10,0,0"/>
            <StackPanel Orientation="Horizontal" Margin="0,5">
                <Button Content="Shop Name" Click="InsertField_Click" Tag="{}{ShopName}" />
                <Button Content="GST Number" Click="InsertField_Click" Tag="{}{GSTNumber}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Button Content="Save Template" Command="{Binding SaveTemplateCommand}" Margin="0,0,10,0" Width="100"/>
                <Button Content="Reset to Default" Command="{Binding ResetTemplateCommand}" Width="100"/>
            </StackPanel>
        </StackPanel>

        <GroupBox Header="Preview" Grid.Column="1" Margin="20,0,0,0">
            <FlowDocumentScrollViewer Document="{Binding PreviewDocument}"/>
        </GroupBox>
    </Grid>
</UserControl>
